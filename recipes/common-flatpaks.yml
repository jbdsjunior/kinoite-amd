modules:
  - type: default-flatpaks
    configurations:
      - scope: user
        notify: false
        repo:
          title: Flathub (user)
        install:
          - com.brave.Browser

      - scope: system
        notify: false
        repo:
          title: Flathub (system)
